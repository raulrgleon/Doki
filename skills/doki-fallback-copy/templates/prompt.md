Fallback output must be safe, short, and fact-free unless the fact is already present in input.

Return only:
{
  "quote": "safe Doki fallback",
  "hint": "safe supporting line",
  "action": "wiggle"
}
