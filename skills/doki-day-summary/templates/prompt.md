The human selected one calendar day.

Use `context.selectedDay` to mention teams, times, and real scores. If no scores are present, create anticipation. If there are finished matches, summarize the day without inventing details.

Return only JSON with `quote`, `hint`, and `action`.
