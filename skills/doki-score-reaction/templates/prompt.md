A score update just arrived.

React to one recent result from `context.recentResults`. Use exact score strings from input. Do not add scorers unless they are included in the context.

Return JSON only.
